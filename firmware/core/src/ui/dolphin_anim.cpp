// Plan 82 Phase 5 — Dolphin animation catalog (paths to .panim files).
// .panim files are provisioned in VFS under "/system/assets/anims/" by the platform at boot.
// Generated by: bun run src/index.ts batch --config asset_gen.config.json
// (Replaces dolphin_showcase.cpp — the 895 KB C-array file is no longer compiled.)
#include "nema/ui/dolphin_anim.h"

namespace nema::anim {

const DolphinEntry DOLPHIN_ENTRIES[] = {
    { "Laptop", "system/assets/anims/laptop.panim", 128, 51, 2 },
};

const size_t DOLPHIN_ENTRIES_COUNT = sizeof(DOLPHIN_ENTRIES) / sizeof(DOLPHIN_ENTRIES[0]);

} // namespace nema::anim
